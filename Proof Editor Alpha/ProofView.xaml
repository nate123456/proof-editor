<UserControl x:Name="userControl" x:Class="Proof_Editor_Alpha.ProofView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Proof_Editor_Alpha"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" BorderThickness="0">
    <local:ExtendedGrid Margin="{Binding proofAreaThickness, Mode=OneWayToSource}" BindableActualHeight="{Binding proofAreaActualHeigh, Mode=OneWayToSource}" BindableActualWidth="{Binding proofAreaActualWidth, Mode=OneWayToSource}" >
        <ItemsControl ItemsSource="{Binding cells}" ScrollViewer.CanContentScroll="True">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                   <local:VirtualizingGrid />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <local:ViewCell />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </local:ExtendedGrid>
</UserControl>
